scalar Time

type Autor @ent {
    id: ID!
    name: String!
    signature: String @unique

    books: [Book!]
}

type Book @ent {
    id: ID!
    createdAt: Time! @default(value: "time.Now")
    updatedAt: Time! @updateDefault(value: "time.Now")

    doi: Float! @default(value: "2.32")
    title: String!
    description: String!

    owner: Autor! @from(ref: "books")
}
