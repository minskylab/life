type Query {
    todos(filter: TodosFilter): [Todo!]!
    summaryTodos: SummaryTodos!
}

type Mutation {
    addTodo(title: String!): Todo!
    deleteTodo(id: ID!): Todo!
}

input TodosFilter {
    how: Int
    first: Int
}

type SummaryTodos @out {
    totalTodos: Int!
    totalPending: Int!
}
